import React, { ReactElement } from 'react';

interface Props {}

function SignInPage({}: Props): ReactElement {
	return <main className='placeholder'>Sign In Page</main>;
}

export default SignInPage;
