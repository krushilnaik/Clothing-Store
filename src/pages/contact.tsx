import React, { ReactElement } from 'react';

interface Props {}

function ContactPage({}: Props): ReactElement {
	return <main className='placeholder'>Contact Page</main>;
}

export default ContactPage;
